# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# Config & validation
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# LLM clients (Groq + Typhoon via OpenAI-compatible APIs)
openai>=1.14.0

# Orchestration + persistent checkpointing.
# Pinned: the checkpoint serializer's allowed-modules behavior is version-sensitive.
langgraph>=1.2.0,<2.0.0
langgraph-checkpoint-sqlite>=3.1.0,<4.0.0

# Document parsing (PDF text extraction; images go to the Groq vision model)
pymupdf>=1.24.0

# Database
sqlalchemy>=2.0.0
# psycopg2-binary>=2.9.0  # uncomment if using PostgreSQL instead of SQLite

# Testing
pytest>=8.1.0
httpx>=0.27.0
